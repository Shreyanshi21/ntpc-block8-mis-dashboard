name: Refresh NTPC user adoption + updates reports from VisiLean

# Runs twice a day (10:15 and 16:15 IST) and whenever someone presses "Run workflow"
# in the Actions tab. Each run is ONE refresh: fetch VisiLean, rebuild, commit and push
# only if the data or the template changed. Same pattern as refresh-v2.yml.
# Builds both the Adoption tracker (/adoption/) and the Updates report (/updates/).
#
# GitHub fires scheduled runs on a best-effort basis, so a scheduled run may start a
# little later than asked (typically 10-30 min, worst case ~2 h). Press "Run workflow"
# if you need the numbers right now.

on:
  schedule:
    # cron is UTC. IST = UTC+5:30. Each project is offset a few minutes from the others
    # so six workers do not all push to the repo at the same second.
    - cron: "45 4 * * *"      # 10:15 IST
    - cron: "45 10 * * *"     # 16:15 IST
  workflow_dispatch:          # manual: Actions tab -> "Run workflow", or `gh workflow run refresh-adoption.yml`

permissions:
  contents: write

concurrency:
  group: refresh-adoption
  cancel-in-progress: true      # a manual run started during a scheduled one replaces it

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # Resolve credentials. The adoption tracker and Updates report read the NTPC
      # project, so they use the NTPC token: VL_TOKEN_NTPC first, then the "ntpc" entry
      # in the single VL_TOKENS_JSON secret. Not configured = warning + skipped refresh,
      # never a failed run - see scripts/sync_preflight.py.
      - name: Resolve VisiLean credentials
        id: pre
        env:
          VL_TOKENS_JSON: ${{ secrets.VL_TOKENS_JSON }}
          VL_TOKEN_NTPC: ${{ secrets.VL_TOKEN_NTPC }}
        run: python scripts/sync_preflight.py adopt

      - name: Refresh once (fetch, build, publish if changed)
        if: steps.pre.outputs.configured == 'true'
        env:
          VL_TOKENS_JSON: ${{ secrets.VL_TOKENS_JSON }}
          VL_TOKEN_NTPC: ${{ secrets.VL_TOKEN_NTPC }}
        run: |
          set -uo pipefail
          git config user.name  "visilean-bot"
          git config user.email "support@visilean.com"

          # VisiLean occasionally blinks; give it three tries two minutes apart before
          # calling the run failed.
          built=false
          for attempt in 1 2 3 ; do
            echo "::group::attempt $attempt  $(date -u '+%H:%M:%S UTC')"
            if python scripts/adoption_data.py && python scripts/build_adoption.py && python scripts/build_updates.py ; then
              built=true
              echo "::endgroup::"
              break
            fi
            echo "attempt $attempt failed (VisiLean unreachable or build error)"
            echo "::endgroup::"
            git checkout -- adoption updates 2>/dev/null || true
            [ "$attempt" -lt 3 ] && sleep 120
          done

          if [ "$built" != "true" ]; then
            echo "::error title=Adoption tracker refresh failed::3 attempts failed - VisiLean unreachable, token rejected, or a build error."
            echo "### Adoption tracker refresh failed" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "All 3 attempts failed. Check the logs above: a rejected token gives HTTP 400 from VisiLean." >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi

          if git diff --quiet -- adoption/.datahash adoption/.tplhash ; then
            echo "no new activity in the trail - nothing published"
            echo "No change in VisiLean data since the last build; nothing published." >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          git add adoption updates/index.html updates/meta.json updates/.datahash
          git commit -m "adoption + updates auto-refresh $(TZ=Asia/Kolkata date '+%d-%b %H:%M IST')" || true
          # the other projects publish at about the same time, so expect a few rejected pushes
          for try in 1 2 3 4 5 ; do
            git pull --rebase --autostash -q && git push -q && { echo "published"; echo "Published a new build at $(TZ=Asia/Kolkata date '+%d-%b %H:%M IST')." >> "$GITHUB_STEP_SUMMARY"; exit 0; }
            echo "push retry $try"; sleep $((10*try))
          done
          echo "::error::could not push after 5 tries"
          exit 1
